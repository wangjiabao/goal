# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Game API
    version: 0.0.1
paths:
    /api/display_game/{type}:
        get:
            tags:
                - Game
            operationId: Game_DisplayGame
            parameters:
                - name: type
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DisplayGameReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/game/sorts:
        get:
            tags:
                - Game
            operationId: Game_GetGameSortList
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetGameSortListReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/games:
        get:
            tags:
                - Game
            operationId: Game_GetGameList
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetGameListReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/team/list:
        get:
            tags:
                - Game
            operationId: Game_GetTeamList
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetTeamListReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        DisplayGameReply:
            type: object
            properties:
                gameId:
                    type: integer
                    format: int64
                name:
                    type: string
                teams:
                    type: array
                    items:
                        $ref: '#/components/schemas/DisplayGameReply_Team'
        DisplayGameReply_Team:
            type: object
            properties:
                teamId:
                    type: integer
                    format: int64
                teamName:
                    type: string
                teamType:
                    type: string
        GetGameListReply:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/GetGameListReply_Item'
        GetGameListReply_Item:
            type: object
            properties:
                gameId:
                    type: integer
                    format: int64
                name:
                    type: string
                startTime:
                    type: string
                endTime:
                    type: string
                upEndTime:
                    type: string
                downStartTime:
                    type: string
                teams:
                    type: array
                    items:
                        $ref: '#/components/schemas/Item_Team'
        GetGameSortListReply:
            type: object
            properties:
                sorts:
                    type: array
                    items:
                        $ref: '#/components/schemas/GetGameSortListReply_Sort'
        GetGameSortListReply_Sort:
            type: object
            properties:
                sortId:
                    type: integer
                    format: int64
                sortType:
                    type: string
                sortName:
                    type: string
                endTime:
                    type: string
        GetTeamListReply:
            type: object
            properties:
                teams:
                    type: array
                    items:
                        $ref: '#/components/schemas/GetTeamListReply_Team'
        GetTeamListReply_Team:
            type: object
            properties:
                teamId:
                    type: integer
                    format: int64
                teamName:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Item_Team:
            type: object
            properties:
                teamId:
                    type: integer
                    format: int64
                teamName:
                    type: string
                teamType:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Game
